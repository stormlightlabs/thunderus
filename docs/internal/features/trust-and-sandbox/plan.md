# Trust and Sandbox

Project-owned runtime configuration requires scoped trust. The sandbox model
separates filesystem and network authority and distinguishes read-only,
workspace-write, and external isolation. Product copy claims only boundaries
an enforcing backend provides.

Built-in shell commands, ACP terminal operations, and MCP server processes use
the same authority vocabulary and approval flow. Descendants settle during
cancellation and shutdown.
