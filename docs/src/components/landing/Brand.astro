<a class="brand" href="/" aria-label="thndrs home">
  <span class="brand__icon i-ri-flashlight-fill" aria-hidden="true"></span>
  <span>thndrs</span>
</a>

<style>
  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.625rem;
    min-height: 2.75rem;
    color: var(--thndrs-fg);
    font-family: var(--thndrs-font-mono);
    font-size: 1rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    text-decoration: none;
  }

  .brand__icon {
    width: 1.25rem;
    height: 1.25rem;
    color: var(--thndrs-green);
    filter: drop-shadow(0 0 0.5rem color-mix(in srgb, var(--thndrs-green) 28%, transparent));
  }
</style>
