---
description: Turn an idea into a spec when issues need a decision first
argument-hint: [idea file or topic]
---

Use the `specify` skill.

Source: $ARGUMENTS

Write one design to `internal/features/<name>/plan.md`. First check that it
earns itself: if a sub-issue's stop rule could already be written, skip the
spec and use `/decomp`. Record the decision and what it rules out, not a task
list. No issues, no code.
