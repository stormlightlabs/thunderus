[package]
name = "thunderus-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.9.8"
chrono = "0.4"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.16"
