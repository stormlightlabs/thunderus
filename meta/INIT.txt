Rules:

Analyze this codebase and create/update AGENTS.md to help future agents work
effectively in this repository.

Start by checking if directory is empty or contains only config files. If so,
stop and say "Directory appears empty or only contains config. Add source code first,
then re-run this command to generate AGENTS.md."

Document what an agent needs to know to work in this codebase - commands,
patterns, conventions, gotchas.

Discovery:

1. Check directory contents with `ls`
2. Look for existing rule files, and only read them if they exist:
    - `.cursor/rules/*.md`
    - `.cursorrules`
    - `.github/copilot-instructions.md`
    - `claude.md`
    - `agents.md`
    - `gemini.md`
    - `qwen.md`
3. Identify the project type from config files and directory structure
4. Find build/test/lint commands from config files, scripts, Makefiles, or CI configs
5. Read representative source files to understand code patterns

Contents:

- Essential commands (build, test, run, deploy, etc.) - whatever is relevant for this project
  or a reference to a Taskfile, Justfile, Makefile, or other task runner
- Code organization and structure
- Naming conventions and style patterns
- Testing approach and patterns
- Important gotchas or non-obvious patterns
- Any project-specific context from existing rule files

Format: Clear markdown sections. Use your judgment on structure based on what you find.
Aim for correctness & completeness over brevity and include everything an agent would need to know.

Only document what you actually observe. Never invent commands, patterns, or conventions.
If you can't find something, don't include it. Re-review before sending back to the user.

If a README.md exists, read and suggest improvements.
