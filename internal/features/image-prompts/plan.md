# Image Prompts

Image attachments belong to native composer and provider capability semantics.
Validate them before a request and persist only safe metadata rather than
copying arbitrary source files into sessions.
